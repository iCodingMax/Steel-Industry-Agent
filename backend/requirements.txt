# ==================== Web 框架 ====================
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# ==================== ORM & 数据库驱动 ====================
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0          # PostgreSQL 异步驱动（系统库 + 向量库）
aiomysql>=0.2.0          # MySQL 异步驱动（业务库）
psycopg2-binary>=2.9.9   # PostgreSQL 同步驱动（pgvector 建表用）
pyodbc>=5.1.0            # SQL Server 驱动
oracledb>=2.1.0          # Oracle 驱动（NL2SQL 执行用）

# ==================== 数据处理 ====================
sqlglot>=23.0.0          # SQL 方言转换（NL2SQL 跨库适配）

# ==================== 配置 & 校验 ====================
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# ==================== 认证 & 安全 ====================
python-jose[cryptography]>=3.3.0  # JWT 令牌
passlib[bcrypt]>=1.7.4             # 密码加密

# ==================== 缓存 ====================
redis[hiredis]>=5.0.0

# ==================== HTTP 客户端 ====================
httpx>=0.27.0            # 异步 HTTP 客户端（调用 Xinference）
aiohttp>=3.9.0           # 异步 HTTP（LlamaIndex 依赖）

# ==================== 日志 ====================
loguru>=0.7.2

# ==================== LlamaIndex（RAG 框架） ====================
llama-index>=0.11.0
llama-index-vector-stores-postgres>=0.2.0   # pgvector 向量存储
llama-index-embeddings-openai>=0.2.0         # OpenAI 兼容嵌入（Xinference）
llama-index-llms-openai>=0.2.0               # OpenAI 兼容 LLM（Xinference）

# ==================== 文档解析 ====================
pypdf>=4.0.0             # PDF 文本提取
python-docx>=1.1.0       # Word 文档解析

# ==================== 开发依赖 ====================
pytest>=8.2.0
pytest-asyncio>=0.23.0
black>=24.4.0
isort>=5.13.0
mypy>=1.10.0
